<resources>

    <style name="AppTheme" parent="AppThemeBase">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppThemeBase" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="MyCustomPopupTheme" parent="ThemeOverlay.AppCompat.Light"></style>

    <style name="MyCustomAppBarTheme" parent="ThemeOverlay.AppCompat.Dark"></style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"></style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"></style>

    <style name="DrawerLayoutTheme" parent="AppTheme"></style>

    <style name="MyCustomProgressBarTheme" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/progress</item>
    </style>

    <style name="MyCustomButtonTheme">
        <item name="android:padding">@dimen/content_padding</item>
        <item name="android:background">@drawable/button_background_selector</item>
    </style>

</resources>
